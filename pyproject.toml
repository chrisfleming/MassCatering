[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mass-catering"
version = "0.1.0"

[tool.setuptools]
packages = ["mass_catering"]


dependencies = [
    "PyYAML",
    "pint>=0.25",
    "reportlab",
]